name: ION Media Python CI

on:
  push:
    branches:
      - "**"
    paths:
      - "ION_media/**"
      - ".github/workflows/ION-media-python-ci.yml"
    paths-ignore:
      - "**/*.md"
      - "docs/**"
      - "codex/**"
  pull_request:
    paths:
      - "ION_media/**"
      - ".github/workflows/ION-media-python-ci.yml"
    paths-ignore:
      - "**/*.md"
      - "docs/**"
      - "codex/**"

jobs:
  validate-ION-media-python:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r ./ION_media/requirements-test.txt

      - name: Run ION media integration tests
        run: python -m unittest discover -s ION_media/tests -p "test_*.py"
