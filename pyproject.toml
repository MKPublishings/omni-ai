[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ION_ai"
version = "0.1.0"
description = "ION Ai Python client for unified cognitive routing via Cloudflare backend"
readme = "README.md"
requires-python = ">=3.9"
authors = [
  { name = "ION Ai Team" }
]
dependencies = [
  "httpx>=0.27.0"
]

[project.urls]
Homepage = "https://mkION.com"
Repository = "https://github.com/MKPublishings/ION-ai"

[tool.setuptools.packages.find]
where = ["."]
include = ["ION_ai*"]